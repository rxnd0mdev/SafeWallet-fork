# .github/workflows/v3_pipeline.yml
name: SafeWallet V3 Enterprise CI/CD Pipeline

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

env:
  NODE_VERSION: '22'
  SNYK_SEVERITY: 'high'

jobs:
  # ==========================================================================
  # Job 1: SAST & Dependency Scanning
  # ==========================================================================
  sast-scan:
    name: Security Scanning (Snyk)
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Verify Snyk Token
        id: check-token
        run: |
          if [ -z "${{ secrets.SNYK_TOKEN }}" ]; then
            echo "::warning::SNYK_TOKEN is not configured in secrets! Snyk scans will be skipped."
            echo "token-set=false" >> $GITHUB_OUTPUT
          else
            echo "✓ SNYK_TOKEN is present"
            echo "token-set=true" >> $GITHUB_OUTPUT
          fi
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

      - name: Install Snyk CLI
        if: steps.check-token.outputs.token-set == 'true'
        run: npm install -g snyk@latest

      - name: Authenticate Snyk
        if: steps.check-token.outputs.token-set == 'true'
        id: snyk-auth
        run: |
          echo "Authenticating Snyk..."
          snyk auth ${{ secrets.SNYK_TOKEN }} || { echo "::error::Authentication failed (401/403)"; exit 1; }
          snyk auth --check || { echo "::error::Snyk auth check failed"; exit 1; }
          echo "✓ Authentication verified successfully"
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

      - name: Run Snyk Open Source Scan
        if: steps.check-token.outputs.token-set == 'true'
        id: snyk-os
        run: |
          echo "Running Snyk Open Source scan..."
          cat > snyk-os.sarif << 'EOF'
          {
            "$schema": "https://raw.githubusercontent.com/oasis-tcs/sarif-spec/master/Schemata/sarif-schema-2.1.0.json",
            "version": "2.1.0",
            "runs": [ { "tool": { "driver": { "name": "Snyk Open Source", "rules": [] } }, "results": [] } ]
          }
          EOF
          snyk test --severity-threshold=${{ env.SNYK_SEVERITY }} --sarif-file-output=snyk-os.sarif --org=${{ secrets.SNYK_ORG_ID }} || true
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

      - name: Run Snyk Code Scan
        if: steps.check-token.outputs.token-set == 'true'
        id: snyk-code
        run: |
          echo "Checking if Snyk Code is enabled..."
          cat > snyk-code.sarif << 'EOF'
          {
            "$schema": "https://raw.githubusercontent.com/oasis-tcs/sarif-spec/master/Schemata/sarif-schema-2.1.0.json",
            "version": "2.1.0",
            "runs": [ { "tool": { "driver": { "name": "Snyk Code", "rules": [] } }, "results": [] } ]
          }
          EOF
          if snyk code test --severity-threshold=${{ env.SNYK_SEVERITY }} --sarif-file-output=snyk-code.sarif --org=${{ secrets.SNYK_ORG_ID }}; then
            echo "✓ Snyk Code scan completed successfully"
          else
            EXIT_CODE=$?
            if [ $EXIT_CODE -eq 2 ]; then
              echo "::warning::Snyk Code is not enabled for your organization."
            fi
          fi
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

      - name: Upload Snyk Results
        uses: github/codeql-action/upload-sarif@v3
        if: always() && steps.check-token.outputs.token-set == 'true'
        with:
          sarif_file: snyk-os.sarif
          category: snyk-os

  # ==========================================================================
  # Job 2: Build & Test
  # ==========================================================================
  build-and-test:
    name: Build & Test
    runs-on: ubuntu-latest
    needs: sast-scan
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci --legacy-peer-deps
      
      - name: NestJS Build & Test
        run: |
          cd v3/gateway-nest
          npm install --legacy-peer-deps
          npm test -- --coverage
      
      - name: Rust Build & Test
        run: |
          if [ -d "v3/security-rust" ]; then
            cd v3/security-rust
            cargo test
          else
            echo "v3/security-rust not found, skipping..."
          fi
      
      - name: Python Build & Test
        run: |
          if [ -d "v3/worker-python" ]; then
            cd v3/worker-python
            pip install -r requirements.txt
            python -m unittest discover .
          else
            echo "v3/worker-python not found, skipping..."
          fi

  # ==========================================================================
  # Job 3: Container Scanning
  # ==========================================================================
  container-scan:
    name: Container Security
    needs: build-and-test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build image
        run: |
          if [ -f "v3/gateway-nest/Dockerfile" ]; then
            docker build -t safewallet-gateway:latest v3/gateway-nest/
          else
            echo "Dockerfile not found, skipping build..."
          fi

  # ==========================================================================
  # Job 4: Deploy
  # ==========================================================================
  deploy-production:
    name: Deploy to Production
    needs: container-scan
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4
      - name: Deploy
        run: |
          echo "Deploying to production..."
          if [ -f "./v3/scripts/validate_deployment.sh" ]; then
            chmod +x ./v3/scripts/validate_deployment.sh
            ./v3/scripts/validate_deployment.sh
          else
            echo "Validation script not found, skipping..."
          fi
